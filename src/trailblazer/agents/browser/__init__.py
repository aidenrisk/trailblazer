"""The browser toolkit agents and Loop share: a live tab, and what it takes to log in on one.

Not an agent. Scraper reads through it, FormFiller acts through it, Validator's
runner replays through it, Loop opens it. A library both agents import is not an
agent-to-agent call.
"""
